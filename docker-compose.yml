version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    volumes:
      - ./backend:/app
    working_dir: /app
    environment:
      - NODE_ENV=development

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
